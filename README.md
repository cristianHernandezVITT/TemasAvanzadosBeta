# TemasAvanzadosCalidad

## Requisitos Previos

Asegúrate de tener instalado:
- [Node.js](https://nodejs.org/) (versión recomendada: 18 o superior)
- npm (normalmente incluido con Node.js)

## Instalación y Ejecución del Proyecto

Para instalar las dependencias y correr el proyecto, ejecuta los siguientes comandos en tu terminal:

```bash
npm install
npm run dev
